# Changelog

## 0.1.0

- Initial public-ready snapshot.
- Supports scanning common macOS cache and development artifact locations.
- Includes dry-run-first cleanup workflows and native CLI commands with stable `--json` output: `info`, `capabilities`, `scan`, and `clean`.
